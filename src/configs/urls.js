const baseURL = 'https://api.themoviedb.org/3'

const urls = {
    // movies: `/movie/{movie_id}/lists`
    movies: `/discover/movie?page=1`
    // movies:`movie/{movie_id}/lists`
}

export {
    baseURL,
    urls
}